services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: seriesrank123
      MYSQL_DATABASE: seriesrank
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
